services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: dogathon
      POSTGRES_PASSWORD: dogathon
      POSTGRES_DB: dogathon
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dogathon -d dogathon"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
